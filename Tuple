# Tupple
# Tuples cannot be changed or modified
# Used for data that will never change. for example (coordinates)

coordinates = [(4, 5), (6, 7), (80, 34)]
# coordinates[1] = 10    #cannot do this because tuples are immutable (cant change)

print(coordinates[0])
